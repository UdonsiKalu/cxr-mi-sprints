# Sprint 2026-W38 — one Qwen, observable from Jupyter

- **Dates:** 2026-09-15 → 2026-09-21
- **Issue:** #1
- **Nanda stage:** mini-project (tooling + first observation)
- **Question:** Can we read last-token residuals in the teaching notebooks without `from_pretrained` a second Qwen?
- **Why this question:** Two copies blew VRAM (GPU + CPU offload). Capture (`look.cmd_capture`) needs a real forward. The GUIs and Jupyter must share **one** in-memory model.

## Ran

- Shared model service `:8270` (`Qwen/Qwen2.5-7B-Instruct`, ~14.2 GiB).
- JupyterLab `:8266`; all tabs pinned to one kernel; `load_model=False`.
- `look.cmd_capture` / `look.cmd_positions` call `:8270` when the kernel has `model is None`.
- Teaching note: *Patient received FOLFOX. Disease progressed. FOLFOX was discontinued.* Layer **20**.

## Found

- Capture (short probe note): last-token L20 L2 ≈ **125.9**, dim **3584**.
- Positions on the FOLFOX note: per-token L20 L2 on the order of **10²** after the first token (first-token L2 is much larger — expected for BOS-like / first position, not a “FOLFOX circuit” claim).

## Claim

- We **can** say: one process owns the weights; Jupyter can print last-token and per-token residual L2 at L20 for this note.
- We **cannot** say: L20 “represents” FOLFOX, that L2 is “layer 2”, or that any direction is causal. **L2 = Euclidean norm.** Observe only.

## Next

- Continue: write a clean positions / layer-sweep table for the same note (sprint 2026-W39).
- Pivot later if the question is only infra.
